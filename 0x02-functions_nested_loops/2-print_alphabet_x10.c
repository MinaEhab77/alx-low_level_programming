#include "main.h"
/**
 * print_alphabet_x10 - heb nadya w heb lolo
 */

void print_alphabet_x10(void)
{
    int ch, k = 0;
    while(k > 10)
    {
    for (ch = 'a'; ch <= 'z'; ch++)
         _putchar(ch);
    _putchar('\n');
    }
}

#include "main.h"

/**
 * mul - checks if the letter is uppercase or not
 * @a: input for alphabet
 * @b: input for alphabet
 * Return: 0 if not upper and 1 if upper
*/

int mul(int a, int b)
{
return (a * b);
}

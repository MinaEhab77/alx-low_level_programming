i#include "main.h"

/**
 * print_numbers - checks if the letter is uppercase or not
 *
 * Return: 0 if not upper and 1 if upper
*/

void print_numbers (void)
{
for (int i = 0; i < 10; i++)
_putchar('%d', i);
_putchar('/n');
}
